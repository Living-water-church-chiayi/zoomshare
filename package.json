{
  "name": "lingxiu-cover",
  "version": "1.0.13",
  "description": "靈修封面 — 晨更/靈修班開始前的 16:9 封面與背景音樂/敬拜播放工具 (Windows/Mac)",
  "main": "src/main.js",
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "electron .",
    "test": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/app.js && node --check src/renderer/bible.js && node --check scripts/test-source.js && node --check scripts/test-runtime.js && node --check scripts/test-reading-layout.js && node --check scripts/test-packaged-smoke.js && node scripts/test-source.js && node scripts/test-runtime.js",
    "test:layout": "electron scripts/test-reading-layout.js",
    "test:packaged": "node scripts/test-packaged-smoke.js",
    "build": "electron-builder",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "publish:win": "electron-builder --win --publish always",
    "publish:mac": "electron-builder --mac --publish always"
  },
  "devDependencies": {
    "electron": "43.1.1",
    "electron-builder": "26.15.3"
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
